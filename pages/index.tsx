import UserInformations from '@/components/UserInformations';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div>
      <UserInformations />
    </div>
  );
};

export default Home;
